import 'package:flutter/material.dart';

class StandartSnackBar extends StatelessWidget {
  const StandartSnackBar({Key? key}) : super(key: key);

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}